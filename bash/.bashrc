# Programming language variables
export PATH=$PATH:~/.cabal/bin/:~/.gem/ruby/2.1.0/bin

# Personal script files
export PATH=$PATH:~/bin # add ~/bin to PATH

# Aliases

# network connections
alias swTGO="sudo netctl switch-to wlp3s0-The\ Good\ One"
alias swNUw="sudo netctl switch-to wlp3s0-NUwave"

alias htmlLoc="python -m http.server"
export EDITOR=ec
export VISUAL=ec

alias ec='emacsclient -tc -a ""'
